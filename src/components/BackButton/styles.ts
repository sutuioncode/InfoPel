import {ViewStyle} from 'react-native';
import styles from '../NextButton/styles';
export default {
  image: {aspectRatio: 1, flex: 1} as ViewStyle,
  container: {
    ...styles.container,
  } as ViewStyle,
};
